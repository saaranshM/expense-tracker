import { FC } from "react";

import LoginScreen from "../components/Login/LoginScreen";

const Login: FC = () => {
  return (
    <section>
      <LoginScreen />
    </section>
  );
};

export default Login;
